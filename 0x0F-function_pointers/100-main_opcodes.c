#include <stdlib.h>
#include <stdio.h>

/**
  * main - prints the opcodes of its own main function.
  * @argc: the number of elements
  * @argv: an array of strings passed in to the function.
  * Return: 1 if number of arguments is incorrect, 2 if number of bytes is
  * negative.
  */
int main(int argc, char *argv[])
{
	printf("Opcodes yo");
	exit(0);
}
