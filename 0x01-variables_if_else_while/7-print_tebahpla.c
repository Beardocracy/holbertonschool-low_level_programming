#include <stdio.h>
/**
  * main - prints all single digit numbers of base 10 starting at 0
  * Return:0
  */

int main(void)
{
	int a;

	for (a = 122; a > 96; a--)
	{
		putchar(a);
	}
	putchar('\n');

	return (0);
}
